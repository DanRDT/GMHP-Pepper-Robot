// @ts-check

/**
 * @param {Array<{
 *  name: string;
 *  variants: Array<{
 *    name: string;
 *    price: number;
 *    calories: number;
 *    image: string;
 *    }>
 *  }> } menuItems
 */
export function verifyMenuItems(menuItems) {
  // TODO
  menuItems.forEach(item => {})
}
